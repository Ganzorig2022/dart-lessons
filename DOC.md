> generics nertei dart-iin console template vvsgew.

```bash
dart create -t console generics
```

```
cd generics
dart run
```

> File system watcher. 

```bash
fswatch -o bin/ | xargs -n1 -I{} sh -c 'clear; dart bin/mixins_logic.dart'
```


#### abstraction and encapsulation, two pillars of OOP